---
import { Icon } from "astro-icon";
import { getLatestPosts } from "@utils/blog";
import BlogItem from "@components/blog/BlogItem";
import MainLayout from "@layouts/MainLayout.astro";
import Hero from "@components/layout/home/Hero.astro";
import SectionHeader from "@components/layout/content/SectionHeader";

const posts = await getLatestPosts(5);
---

<MainLayout title="Homepage">
  <Hero />
  <div id="blog" class="space-y-6">
    <SectionHeader title="Blog" description="Sometimes, I write tech stuff" />
    <ul class="space-y-5">
      {posts.map(post => <BlogItem slug={post.slug} {...post.data} />)}
    </ul>
    <div class="text-gray-700">
      <a href="/blog" class="text-sm flex items-center">
        <span>See All Posts</span>
        <Icon class="w-4 ml-2" name="ph:arrow-right" />
      </a>
    </div>
  </div>
</MainLayout>
